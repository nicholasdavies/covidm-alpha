## This assumes that the LIB_GSL variable points to working GSL libraries
PKG_CPPFLAGS = $(SHLIB_OPENMP_CFLAGS) -I$(LIB_GSL)/include
PKG_CXXFLAGS = -std=c++11
PKG_LIBS = $(SHLIB_OPENMP_CFLAGS) -L$(LIB_GSL)/lib -lgsl -lgslcblas
