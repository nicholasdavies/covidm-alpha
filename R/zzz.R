# covidm - zzz.R
# code to execute on attach and on load.

.onAttach = function(libname, pkgname)
{
    # e.g. packageStartupMessage("Welcome to covidm.")
}

.onLoad = function(libname, pkgname)
{
    # e.g. Rcpp::loadModule("name_of_module", TRUE)
}
